--Codificar un procedimiento que reciba una cadena y la visualice al revés.
SET SERVEROUTPUT ON
DECLARE
cuento varchar2(50):= 'Del Reves'
cadena_rev VARCHAR2(20);
BEGIN
FOR I in reverse 1 .. lenght(cadena_rev)
loop
  
end loop

END;